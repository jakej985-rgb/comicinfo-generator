# =================================================================
# ComicInfo Generator Environment Configuration
# =================================================================

# Absolute or relative path to your host comic library directory
# (Container mounts this path to /comics for embedding metadata)
COMICS_PATH=./comics

# Port to expose the Web UI on (default: 5005)
PORT=5005
